//TwitchData includes required twitch information
export interface TwitchTokens {
  username: string;
  password: string;
  clientID: string;
  clientSecret: string;
  accessToken: string;
  refreshToken: string;
}
